---
interface Props {
    label: string;
    id?: string;
    rule?: boolean;
}

const { label, id, rule = true } = Astro.props;
---

<section id={id} class:list={["relative", rule && "border-t border-line"]} data-motion-line>
    <div class="shell relative z-1 grid grid-cols-1 gap-0 py-[clamp(56px,9vw,104px)] md:grid-cols-[clamp(0px,5vw,64px)_minmax(0,1fr)]">
        <div class="relative hidden md:block">
            <span class="sticky top-24 block rotate-180 font-mono text-[8.5px] uppercase tracking-[0.3em] text-ink-faint">
                {label}
            </span>
        </div>
        <div class="min-w-0" data-motion-reveal>
            <slot />
        </div>
    </div>
</section>
